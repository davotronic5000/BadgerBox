import ManageProfile from "@/components/profile/manage-profile";

interface PageProps {}

const Page = ({}: PageProps) => {
    return (
        <div>
            <ManageProfile />
        </div>
    );
};

export default Page;
